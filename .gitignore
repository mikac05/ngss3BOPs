# Host authorization state and raw provider working captures do not transfer.
/.hermes-allow-agy-auto-approve
/scratch_harvest/
# Local dependencies and runtime caches
node_modules/
__pycache__/
*.py[cod]
# Host-local model settings and agent runtime must not be published.
/.codex/
# Agent hygiene runtime (task-local, never source of truth)
.agent/
